# Cloud Run용 ADK 에이전트 이미지 (해커톤 권장: Cloud Run)
FROM python:3.12-slim
WORKDIR /app
COPY agent/pyproject.toml .
RUN pip install --no-cache-dir uv && uv pip install --system -r pyproject.toml
COPY agent/ .
ENV PORT=8080
CMD ["adk", "api_server", "--host", "0.0.0.0", "--port", "8080", "commerce_agent"]
